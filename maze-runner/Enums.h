#pragma once

enum class Action
{
    MOVING,
    LOOKING
};

enum class Direction
{
    FORWARD,
    BACKWARD,
    TURN_LEFT,
    TURN_RIGHT
};

enum class Looking
{
    LEFT,
    RIGHT,
    CENTER
};